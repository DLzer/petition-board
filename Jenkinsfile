pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'php --version'
                sh 'echo "Hello World"'
                sh 'echo "Build Complete"'
            }
        }
    }
}
